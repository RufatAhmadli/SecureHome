spring:
  datasource:
    url: ${JDBC_URL}
    password: ${JDBC_PASSWORD}
    username: ${JDBC_USERNAME}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

jwt:
  secret: ${SECRET_KEY}
  expiration: ${EXPIRATION_TIME}
logging:
  level:
    liquibase: DEBUG
    root: INFO